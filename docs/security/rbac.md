# RBAC

```text
ORG_OWNER  - full organization control
ADMIN      - manages secrets, connections, contracts, rotation, and audit review
MEMBER     - creates and manages allowed secrets
VIEWER     - views allowed metadata and explicitly shared secrets
AUDITOR    - reads metadata and audit logs, cannot reveal secrets
```

