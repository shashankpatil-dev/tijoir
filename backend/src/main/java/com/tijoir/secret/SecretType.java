package com.tijoir.secret;

public enum SecretType {
    PASSWORD,
    API_KEY,
    WEBHOOK_SECRET,
    SSH_PUBLIC_KEY,
    SSH_PRIVATE_KEY,
    SFTP_PASSWORD,
    TOKEN,
    CERTIFICATE,
    CUSTOM
}
