package com.tijoir.audit;

public enum AuditAction {
    SECRET_CREATED,
    SECRET_REVEALED,
    SECRET_REVOKED,
    SECRET_ROTATED,
    SHARE_LINK_CREATED,
    SHARE_LINK_CONSUMED,
    SHARE_LINK_REVOKED
}
